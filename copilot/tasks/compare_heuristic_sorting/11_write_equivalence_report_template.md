```markdown
# Task 11 — Author equivalence report template

Goal
- Create a report template `copilot/reports/heuristic_equivalence_report.md` to capture findings, diffs, and triage notes.

Steps
1. Add the markdown template file with sections: Executive summary, Test set, Results, Notable mismatches, Triage notes, Next steps.
2. Populate initial entries from the deterministic tests.

Verification
- The report file exists and can be updated after runs.

Notes
- Keep the report concise and link into `build/compare-results/<timestamp>/` artifacts.

```
