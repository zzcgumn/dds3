## Task 3: Bazel Wiring

- **Goal:** Set up the Bazel build targets for the new library and its tests.
- **Key Actions:**
    - Create a `BUILD.bazel` file for the `heuristic_sorting` library.
    - Define a `cc_library` target for the new library.
    - Create a `BUILD.bazel` file for the library's tests.
    - Update the main `dds` library to depend on the new `heuristic_sorting` library.
- **Deliverable:** `BUILD.bazel` files for the new library and its tests.
